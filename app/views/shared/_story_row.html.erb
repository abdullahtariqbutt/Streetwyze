<tr id="story_row_<% story.id %>">

  <td><%= story.name %></td>
  <td><%= story.address %></td>
  <td><%= story.category %></td>
  <td><%= story.rating %></td>
  <td><%= story.stuff_type %></td>
  <td><%= story.description %></td>
  <td><%= link_to 'Show', story, class: "btn btn-primary" %></td>

  <!-- User Sign In Check to Allow Edit and Delete -->
  <% if story.user == current_user %>
    <td><%= link_to 'Destroy', story, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
  <% end %>
  <!-- End -->

</tr>
